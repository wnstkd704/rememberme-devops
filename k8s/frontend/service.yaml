apiVersion: v1
kind: Service
metadata:
  name: rememberme-frontend-active
  namespace: rememberme

spec:
  type: ClusterIP

  selector:
    app: rememberme-frontend

  ports:
  - port: 80
    targetPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: rememberme-frontend-preview
  namespace: rememberme

spec:
  type: ClusterIP

  selector:
    app: rememberme-frontend

  ports:
  - port: 80
    targetPort: 80