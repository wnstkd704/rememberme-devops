apiVersion: v1
kind: Service

metadata:
  name: rememberme-backend-active
  namespace: rememberme

spec:
  type: ClusterIP

  selector:
    app: rememberme-backend

  ports:
  - port: 8088
    targetPort: 8088

---
apiVersion: v1
kind: Service

metadata:
  name: rememberme-backend-preview
  namespace: rememberme

spec:
  type: ClusterIP

  selector:
    app: rememberme-backend

  ports:
  - port: 8088
    targetPort: 8088