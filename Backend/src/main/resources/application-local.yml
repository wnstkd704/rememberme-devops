spring:
  jackson:
    serialization:
      write-dates-as-timestamps: false # 날짜를 [2026,3,14]가 아닌 "2026-03-14"로 출력
    default-property-inclusion: non_null # null인 필드는 JSON 응답에서 제외

  # 데이터소스 구조만 정의 (값은 각 프로필에서 채움)
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/rememberv1?permitMysqlScheme&user=root&password=0000
    username: root
    password: 0000
  mail:
    host: smtp.gmail.com
    port: 587
    username: junsuk530@gmail.com   # (시스템 메일 발송 주체가 될 계정)
    password: wpun woul phij nonq        # (발급받은 16자리 앱 비밀번호 기입)
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

jwt:
  secret: dunoesanchaeg-local-development-secret-key-2026-remember-me

mybatis:
  # 패키지 경로는 프로젝트 구조에 맞춰 수정 가능
  type-aliases-package: com.rememberme.dunoesanchaeg.calendar.*.model.vo
  mapper-locations: classpath:mapper/**/*.xml
  configuration:
    jdbc-type-for-null: 'NULL'
    map-underscore-to-camel-case: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha

server:
  port: 8088

app:
  cors:
    allowed-origins: https://brainwalk.click,http://localhost:5173